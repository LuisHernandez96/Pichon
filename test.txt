FUNCTIONS{

}

ENVIRONMENT{
    spawnObject(sphere, 3, 0, 0);
    spawnObject(sphere, ~1, 0, 0);
    spawnObject(sphere, 3, 5, 0);
    spawnObject(sphere, 3, 3, 0);
    spawnObject(cube, 13, 5, 10);
    spawnObject(cube, 3, 5, 10);
    start(8, 5, 10);
    goal(0, 0, 0);
}

MOVEMENT{
    
    while(true){
        while(canMoveForward()){
            forward();
        };

        turnLeft();
        turnLeft();
    };
}